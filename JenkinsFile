pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                script{
                    common = load "common.groovy"
                    common.updateGithubStatus("INPROGRESS")
                    sleep 30
                    echo "test"
                }
            } 
        
            // post {
            //     steps{
            //         script{
            //             if ("${currentBuild.currentResult}" == 'SUCCESS') {
            //                 common.updateGithubStatus("SUCCESSFUL")
            //             } else {
            //                 common.updateGithubStatus("FAILED")
            //             }
            //         }
            //     }
            // }
        }
    }
}
